import React from 'react';
import { HomePage } from './presentation/pages/HomePage';

/**
 * App — root component.
 * Currently renders HomePage directly. Can be extended with a router
 * (e.g. React Router) for multi-page support.
 */
function App() {
  return <HomePage />;
}

export default App;
